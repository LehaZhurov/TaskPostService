#id для операций над определенным постом
PathId:
  in: path
  name: id
  required: true
  schema:
    type: integer
    minimum: 1
  description: Числовой id
  example: 1
VoiceId:
  in: path
  name: voiceId
  required: true
  schema:
    type: integer
    minimum: 1
  description: Числовой id
  example: 1
#Описание по офрмлению сущностей для добавления ответа
QueryInclude:
  in: query
  name: include
  required: false
  description: Связанные сущности для подгрузки, через запятую
  schema:
    type: string